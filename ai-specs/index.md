# Spec Index

Manifest of all specs for `com.onthehill.xaero-world-map-book`. See `ai-specs/standards/README.md` for the pipeline this table supports and `ai-specs/standards/rules/spec-workflow.md` for how to update it.

| # | Title | Status | Depends On | Output Files |
|---|-------|--------|------------|---------------|
| 000 | Example Tick Progress Value (Server-Authoritative Rate, Client-Selectable Visualization) | Implemented — **`./gradlew build` passes** (compiles, packages, tests all green), first time confirmed; runtime/visual behavior (config screen, both HUD modes, Mod Menu entry) still needs a manual pass in an actual launchable game client, which this sandbox cannot do — see spec's Post-Implementation Notes | None | `progress/`, `config/`, `network/`, `ModCommands.java`, `client/config/`, `client/network/`, `client/render/`, `client/screen/`, `client/ClientModCommands.java`, `client/XaeroWorldMapBookMenuIntegration.java`, `assets/xaero-world-map-book/lang/en_us.json` |
| 001 | Gate Xaero's World Map / Minimap Access Behind an "Atlas" Item | Ready | None | `item/`, `config/MapAccessConfig.java`, `config/KeybindItemRequirement.java`, `config/MinimapAccessRequirement.java`, `network/MapAccess*.java`, `command/MapAccessCommands.java`, `progression/`, `datagen/AtlasRecipeProvider.java`, `datagen/ModAdvancementProvider.java`, `client/config/MapAccessClientState.java`, `client/network/MapAccessClientNetworkHandler.java`, `client/screen/MapAccessConfigScreen.java`, `client/integration/XaeroWorldMapBridge.java`, `client/mixin/XaeroWorldMapKeybindMixin.java`, `client/mixin/XaeroMinimapVisibilityMixin.java` |
| 002 | Change Xaero's World Map / Minimap In-Game Appearance | Not Started | 001 | TBD |

Spec 000 is this template repository's own worked example (see `ai-specs/specs/000-tick-progress-example.md`'s header note and `CLAUDE.md`'s "Worked Example: Spec 000" section) — it is not queued work for this mod's real feature set, and is being kept in place as scaffold rather than deleted (see `CLAUDE.md`). This repo's real work starts at `001`: gating access to Xaero's World Map and Xaero's Minimap behind in-world items, and `002`: changing how those maps present in-game. Author each from `ai-specs/standards/template.md`, using Spec 000 as the concrete structural example rather than that Unity-flavored template alone.
